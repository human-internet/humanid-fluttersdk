import 'package:flutter/material.dart';

class HumanIdSDK extends StatelessWidget {
  static String routeName = '/humanid_sdk';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('HUMAN ID SDK')),
      ),
    );
  }
}
